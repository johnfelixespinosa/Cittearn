<%= form_for(@progression) do |f| %>

  <%= f.label "Add a new chord" %><br>
  <%= f.fields_for :chords, song.chords.build do |chords_fields| %>
    <%= categories_fields.text_field :name %><br>
    <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>